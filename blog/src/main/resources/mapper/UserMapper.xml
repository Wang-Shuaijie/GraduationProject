<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wangshjm.blog.dao.UserMapper">
    <select id="findUserByEmailAndPwd" parameterType="String" resultType="com.wangshjm.blog.entity.User">
        select *
        from user
        where email=#{email} and password=#{password};
    </select>
    <select id="findByEmail" parameterType="String" resultType="com.wangshjm.blog.entity.User">
        select *
        from user
        where email=#{email};
    </select>
    <select id="findByPhone" parameterType="String" resultType="com.wangshjm.blog.entity.User">
        select *
        from user
        where phone=#{phone};
    </select>
</mapper>